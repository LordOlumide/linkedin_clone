import 'package:flutter/material.dart';

class MessagingScreen extends StatelessWidget {
  static const screen_id = 'messaging_screen';

  const MessagingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}